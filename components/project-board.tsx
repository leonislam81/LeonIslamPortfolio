"use client"

import { FormEvent, useState } from "react"
import { Plus, Workflow } from "lucide-react"
import { createSupabaseBrowserClient } from "@/lib/supabase/client"

type Project = { id: string; client_name: string; title: string; status: string; due_date: string | null; value: number; notes: string | null }
const statusStyle: Record<string, string> = { Planned: "bg-slate-100 text-slate-700", "In progress": "bg-sky-100 text-sky-800", Waiting: "bg-amber-100 text-amber-800", Completed: "bg-emerald-100 text-emerald-800" }

export function ProjectBoard({ initialProjects }: { initialProjects: Project[] }) {
  const [projects, setProjects] = useState(initialProjects)
  const [showForm, setShowForm] = useState(false)
  const [state, setState] = useState<"idle" | "saving" | "error">("idle")
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setState("saving"); const form = new FormData(event.currentTarget); const supabase = createSupabaseBrowserClient(); if (!supabase) return setState("error"); const { data: { user } } = await supabase.auth.getUser(); if (!user) return setState("error"); const { data, error } = await supabase.from("projects").insert({ owner_id: user.id, client_name: String(form.get("client_name")), title: String(form.get("title")), status: String(form.get("status")), due_date: String(form.get("due_date")) || null, value: Number(form.get("value") || 0), notes: String(form.get("notes")) || null }).select("id, client_name, title, status, due_date, value, notes").single(); if (error || !data) return setState("error"); setProjects((current) => [data, ...current]); setShowForm(false); setState("idle"); event.currentTarget.reset() }
  return <div className="mt-8 space-y-5"><div className="flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-slate-600">Create projects for confirmed work, then expand this space with tasks, files, and client updates.</p><button type="button" onClick={() => setShowForm((value) => !value)} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-sky-700 px-4 text-sm font-semibold text-white"><Plus className="size-4" />New project</button></div>{showForm && <form onSubmit={submit} className="grid gap-3 rounded-2xl border border-sky-200 bg-sky-50 p-5 sm:grid-cols-2"><input name="client_name" required placeholder="Client name" className="min-h-11 rounded-xl border border-slate-200 px-3 text-sm" /><input name="title" required placeholder="Project title" className="min-h-11 rounded-xl border border-slate-200 px-3 text-sm" /><select name="status" className="min-h-11 rounded-xl border border-slate-200 bg-white px-3 text-sm"><option>Planned</option><option>In progress</option><option>Waiting</option><option>Completed</option></select><input name="due_date" type="date" className="min-h-11 rounded-xl border border-slate-200 px-3 text-sm" /><input name="value" type="number" min="0" step="0.01" placeholder="Project value" className="min-h-11 rounded-xl border border-slate-200 px-3 text-sm" /><textarea name="notes" placeholder="Project notes" className="min-h-11 rounded-xl border border-slate-200 p-3 text-sm" /><div className="sm:col-span-2 flex gap-3"><button disabled={state === "saving"} className="rounded-xl bg-sky-700 px-4 py-3 text-sm font-semibold text-white">{state === "saving" ? "Saving..." : "Create project"}</button>{state === "error" && <p className="self-center text-sm text-rose-700">Could not save. Run the projects SQL first.</p>}</div></form>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.map((project) => <article key={project.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-slate-500">{project.client_name}</p><h2 className="mt-1 text-lg font-bold">{project.title}</h2></div><span className={`rounded-full px-2 py-1 text-xs font-semibold ${statusStyle[project.status]}`}>{project.status}</span></div>{project.notes && <p className="mt-4 text-sm leading-6 text-slate-600">{project.notes}</p>}<div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-sm"><span className="text-slate-500">{project.due_date ? `Due ${new Date(`${project.due_date}T00:00:00`).toLocaleDateString()}` : "No due date"}</span><span className="font-bold">{project.value || 0}</span></div></article>)}</div>{!projects.length && <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><Workflow className="mx-auto size-7 text-sky-700" /><h2 className="mt-4 text-lg font-bold">No projects yet</h2><p className="mt-2 text-sm text-slate-600">Create your first confirmed client project from here.</p></div>}</div>
}
