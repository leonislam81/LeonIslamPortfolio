"use client"
import { FormEvent, useEffect, useState } from "react"
import { CalendarClock, CheckCircle2, Plus } from "lucide-react"
import { createSupabaseBrowserClient } from "@/lib/supabase/client"
type Task = { id: string; title: string; completed: boolean; due_date: string | null }
export function ProjectTaskList({ projectId, initialTasks }: { projectId: string; initialTasks: Task[] }) { const [tasks, setTasks] = useState(initialTasks); const [title, setTitle] = useState(""); const [dueDate, setDueDate] = useState(""); useEffect(() => { const load = async () => { const supabase = createSupabaseBrowserClient(); if (!supabase) return; const { data } = await supabase.from("project_tasks").select("id,title,completed,due_date").eq("project_id", projectId).order("created_at"); if (data) setTasks(data) }; void load() }, [projectId]); const add = async (e: FormEvent) => { e.preventDefault(); const supabase = createSupabaseBrowserClient(); if (!supabase || !title.trim()) return; const { data: { user } } = await supabase.auth.getUser(); if (!user) return; const { data } = await supabase.from("project_tasks").insert({ project_id: projectId, owner_id: user.id, title, due_date: dueDate || null }).select("id,title,completed,due_date").single(); if (data) { setTasks([...tasks, data]); setTitle(""); setDueDate("") } }; const toggle = async (task: Task) => { const supabase = createSupabaseBrowserClient(); if (!supabase) return; const completed = !task.completed; setTasks(tasks.map((item) => item.id === task.id ? { ...item, completed } : item)); await supabase.from("project_tasks").update({ completed }).eq("id", task.id) }; const complete = tasks.filter((task) => task.completed).length; const percent = tasks.length ? Math.round((complete / tasks.length) * 100) : 0; const ordered = [...tasks].sort((a, b) => Number(a.completed) - Number(b.completed) || (a.due_date || "9999").localeCompare(b.due_date || "9999")); return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2"><CheckCircle2 className="size-5 text-sky-700" /><h2 className="text-xl font-bold">Project tasks</h2></div><span className="text-sm font-semibold text-slate-500">{complete}/{tasks.length} done</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-100"><span className="block h-full rounded-full bg-sky-600 transition-all" style={{ width: `${percent}%` }} /></div><form onSubmit={add} className="mt-5 grid gap-2 sm:grid-cols-[1fr_auto_auto]"><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Add a task" className="min-h-11 rounded-xl border border-slate-200 px-3 text-sm" /><input type="date" value={dueDate} onChange={(e) => setDueDate(e.target.value)} className="min-h-11 rounded-xl border border-slate-200 px-3 text-sm" /><button className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-sky-700 px-4 text-sm font-semibold text-white"><Plus className="size-4" />Add</button></form><div className="mt-5 space-y-2">{ordered.map((task) => <label key={task.id} className="flex items-center gap-3 rounded-xl bg-slate-50 p-3 text-sm"><input type="checkbox" checked={task.completed} onChange={() => toggle(task)} className="size-4 accent-sky-700" /><span className={`min-w-0 flex-1 ${task.completed ? "text-slate-400 line-through" : "font-medium"}`}>{task.title}</span>{task.due_date && <span className="inline-flex shrink-0 items-center gap-1 text-xs font-medium text-slate-500"><CalendarClock className="size-3.5" />{new Date(`${task.due_date}T00:00:00`).toLocaleDateString()}</span>}</label>)}{!tasks.length && <p className="text-sm text-slate-500">No tasks yet.</p>}</div></section> }
