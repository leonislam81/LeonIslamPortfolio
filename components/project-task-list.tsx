"use client"
import { FormEvent, useEffect, useState } from "react"
import { CheckCircle2, Plus } from "lucide-react"
import { createSupabaseBrowserClient } from "@/lib/supabase/client"
type Task = { id: string; title: string; completed: boolean; due_date: string | null }
export function ProjectTaskList({ projectId, initialTasks }: { projectId: string; initialTasks: Task[] }) { const [tasks, setTasks] = useState(initialTasks); const [title, setTitle] = useState(""); useEffect(() => { const load = async () => { const supabase = createSupabaseBrowserClient(); if (!supabase) return; const { data } = await supabase.from("project_tasks").select("id,title,completed,due_date").eq("project_id", projectId).order("created_at"); if (data) setTasks(data) }; void load() }, [projectId]); const add = async (e: FormEvent) => { e.preventDefault(); const supabase = createSupabaseBrowserClient(); if (!supabase || !title.trim()) return; const { data: { user } } = await supabase.auth.getUser(); if (!user) return; const { data } = await supabase.from("project_tasks").insert({ project_id: projectId, owner_id: user.id, title }).select("id,title,completed,due_date").single(); if (data) { setTasks([...tasks, data]); setTitle("") } }; const toggle = async (task: Task) => { const supabase = createSupabaseBrowserClient(); if (!supabase) return; const completed = !task.completed; setTasks(tasks.map((item) => item.id === task.id ? { ...item, completed } : item)); await supabase.from("project_tasks").update({ completed }).eq("id", task.id) }; const complete = tasks.filter((task) => task.completed).length; return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2"><CheckCircle2 className="size-5 text-sky-700" /><h2 className="text-xl font-bold">Project tasks</h2></div><span className="text-sm font-semibold text-slate-500">{complete}/{tasks.length} done</span></div><form onSubmit={add} className="mt-5 flex gap-2"><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Add a task" className="min-h-11 flex-1 rounded-xl border border-slate-200 px-3 text-sm" /><button className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-sky-700 px-4 text-sm font-semibold text-white"><Plus className="size-4" />Add</button></form><div className="mt-5 space-y-2">{tasks.map((task) => <label key={task.id} className="flex items-center gap-3 rounded-xl bg-slate-50 p-3 text-sm"><input type="checkbox" checked={task.completed} onChange={() => toggle(task)} className="size-4 accent-sky-700" /><span className={task.completed ? "text-slate-400 line-through" : "font-medium"}>{task.title}</span></label>)}{!tasks.length && <p className="text-sm text-slate-500">No tasks yet.</p>}</div></section> }
