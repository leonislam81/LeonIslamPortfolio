"use client"

import { CalendarCheck2, CheckCircle2, CircleX, Clock3, UserRound } from "lucide-react"
import { useState } from "react"

type Booking = { id: string; booking_uid: string; status: string; guest_name: string | null; guest_email: string | null; guest_timezone: string | null; event_title: string | null; start_time: string | null; end_time: string | null; location: string | null; notes: string | null; created_at: string }

export function DashboardBookingsManager({ initialBookings }: { initialBookings: Booking[] }) {
  const [bookings, setBookings] = useState(initialBookings)
  const [filter, setFilter] = useState("all")
  const visible = bookings.filter((booking) => filter === "all" || booking.status === filter)
  const statusIcon = (status: string) => status === "Confirmed" ? <CheckCircle2 className="size-4" /> : status === "Cancelled" ? <CircleX className="size-4" /> : <Clock3 className="size-4" />
  return <main className="min-h-screen bg-slate-50 px-5 py-8 text-slate-950 sm:px-8"><div className="mx-auto max-w-6xl"><header className="flex flex-wrap items-end justify-between gap-5 border-b border-slate-200 pb-7"><div><p className="text-sm font-bold uppercase tracking-[.16em] text-sky-700">Business pipeline</p><h1 className="mt-2 text-3xl font-bold tracking-tight">Booking Inbox</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-600">Every discovery call booking from Cal.com, including guest details, timing, timezone, notes, and status.</p></div><div className="flex items-center gap-2 rounded-2xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white"><CalendarCheck2 className="size-5 text-sky-300" />{bookings.filter((booking) => booking.status === "Confirmed").length} confirmed</div></header><div className="mt-8 flex flex-wrap items-center justify-between gap-3"><div className="flex gap-2">{["all", "Confirmed", "Cancelled", "Rescheduled"].map((item) => <button key={item} type="button" onClick={() => setFilter(item)} className={`rounded-full px-3 py-2 text-sm font-semibold ${filter === item ? "bg-slate-950 text-white" : "border border-slate-200 bg-white text-slate-600"}`}>{item === "all" ? "All bookings" : item}</button>)}</div><a href="/book-call" target="_blank" rel="noreferrer" className="text-sm font-semibold text-sky-700">Open booking page →</a></div>{visible.length ? <div className="mt-5 grid gap-4 lg:grid-cols-2">{visible.map((booking) => <article key={booking.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-xl bg-sky-100 text-sky-700"><UserRound className="size-5" /></span><div><h2 className="font-bold">{booking.guest_name || "Unnamed guest"}</h2><a href={`mailto:${booking.guest_email ?? ""}`} className="text-sm text-sky-700 hover:underline">{booking.guest_email || "No email provided"}</a></div></div><span className={`inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-xs font-bold ${booking.status === "Confirmed" ? "bg-emerald-100 text-emerald-700" : booking.status === "Cancelled" ? "bg-rose-100 text-rose-700" : "bg-amber-100 text-amber-700"}`}>{statusIcon(booking.status)}{booking.status}</span></div><dl className="mt-5 grid gap-3 rounded-xl bg-slate-50 p-4 text-sm sm:grid-cols-2"><div><dt className="text-xs font-bold uppercase tracking-wide text-slate-400">Event</dt><dd className="mt-1 font-semibold">{booking.event_title || "Project discovery call"}</dd></div><div><dt className="text-xs font-bold uppercase tracking-wide text-slate-400">When</dt><dd className="mt-1 font-semibold">{booking.start_time ? new Date(booking.start_time).toLocaleString() : "Not provided"}</dd></div><div><dt className="text-xs font-bold uppercase tracking-wide text-slate-400">Timezone</dt><dd className="mt-1 font-semibold">{booking.guest_timezone || "Not provided"}</dd></div><div><dt className="text-xs font-bold uppercase tracking-wide text-slate-400">Location</dt><dd className="mt-1 font-semibold">{booking.location || "Cal.com meeting link"}</dd></div></dl>{booking.notes && <div className="mt-4 rounded-xl border border-sky-100 bg-sky-50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-sky-700">Guest notes</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-slate-700">{booking.notes}</p></div>}<p className="mt-4 text-xs text-slate-400">Received {new Date(booking.created_at).toLocaleString()}</p></article>)}</div> : <div className="mt-5 rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center"><CalendarCheck2 className="mx-auto size-8 text-slate-300" /><p className="mt-3 font-semibold">No bookings found.</p><p className="mt-1 text-sm text-slate-500">Connect the Cal.com webhook to start receiving booking details here.</p></div>}</div></main>
}
