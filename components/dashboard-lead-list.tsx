"use client"

import { useMemo, useState } from "react"
import { CalendarClock, Search, SlidersHorizontal } from "lucide-react"
import { LeadStatusSelect } from "@/components/lead-status-select"

export type DashboardLead = { id: string; website_url: string; email: string; status: string; priority: "high" | "normal" | "low"; tags: string[]; performance: number | null; seo: number | null; business_goal: string | null; created_at: string; follow_up_at: string | null; re_audit_at: string | null }
const dateAtMidnight = (value: string) => new Date(`${value}T00:00:00`)
const today = new Date(new Date().getFullYear(), new Date().getMonth(), new Date().getDate())
const plusDays = (days: number) => new Date(today.getFullYear(), today.getMonth(), today.getDate() + days)
function followUp(lead: DashboardLead) { const date = lead.follow_up_at ? dateAtMidnight(lead.follow_up_at) : lead.re_audit_at ? dateAtMidnight(lead.re_audit_at) : null; if (!date) return null; const type = lead.follow_up_at ? "Follow up" : "Re-audit"; return date < today ? { text: `${type} overdue`, style: "bg-rose-50 text-rose-800 ring-rose-200" } : date <= plusDays(7) ? { text: `${type} due soon`, style: "bg-amber-50 text-amber-900 ring-amber-200" } : { text: `${type} ${date.toLocaleDateString()}`, style: "bg-slate-100 text-slate-700 ring-slate-200" } }
const priorityStyle = { high: "bg-rose-100 text-rose-800", normal: "bg-sky-100 text-sky-800", low: "bg-slate-100 text-slate-700" }

export function DashboardLeadList({ leads }: { leads: DashboardLead[] }) {
  const [search, setSearch] = useState("")
  const [status, setStatus] = useState("all")
  const [followUpFilter, setFollowUpFilter] = useState("all")
  const statuses = Array.from(new Set(leads.map((lead) => lead.status))).sort()
  const dueLeads = leads.filter((lead) => { const item = followUp(lead); return item?.text.includes("overdue") || item?.text.includes("due soon") })
  const visible = useMemo(() => leads.filter((lead) => { const phrase = search.trim().toLowerCase(); const text = `${lead.website_url} ${lead.email} ${lead.tags.join(" ")}`.toLowerCase(); const label = followUp(lead)?.text ?? ""; return (!phrase || text.includes(phrase)) && (status === "all" || lead.status === status) && (followUpFilter === "all" || followUpFilter === "overdue" && label.includes("overdue") || followUpFilter === "action" && (label.includes("overdue") || label.includes("due soon"))) }), [followUpFilter, leads, search, status])
  return <div className="mt-8 space-y-5">{dueLeads.length > 0 && <section className="rounded-2xl border border-amber-200 bg-amber-50 p-5"><div className="flex items-center gap-2"><CalendarClock className="size-5 text-amber-700" /><h2 className="font-bold text-amber-950">Follow-up queue</h2><span className="rounded-full bg-amber-200 px-2 py-0.5 text-xs font-bold text-amber-950">{dueLeads.length}</span></div><p className="mt-2 text-sm text-amber-900">These leads have a follow-up due now or within the next seven days.</p><div className="mt-4 flex flex-wrap gap-2">{dueLeads.slice(0, 5).map((lead) => <a key={lead.id} href={`/dashboard/leads/${lead.id}`} className="rounded-xl border border-amber-200 bg-white px-3 py-2 text-sm font-semibold text-amber-950 transition hover:border-amber-400">{lead.website_url}</a>)}</div></section>}<section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-wrap items-center gap-2 border-b border-slate-200 p-5"><SlidersHorizontal className="size-5 text-sky-700" /><h2 className="font-bold">Lead pipeline</h2><p className="ml-auto text-xs text-slate-500">{visible.length} of {leads.length} leads shown</p></div><div className="grid gap-3 border-b border-slate-100 p-5 md:grid-cols-[1fr_auto_auto]"><label className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search website, email, or tag" className="min-h-11 w-full rounded-xl border border-slate-200 py-2 pl-10 pr-3 text-sm outline-none focus:border-sky-500 focus:ring-4 focus:ring-sky-100" /></label><select aria-label="Filter by status" value={status} onChange={(event) => setStatus(event.target.value)} className="min-h-11 rounded-xl border border-slate-200 bg-white px-3 text-sm font-medium"><option value="all">All statuses</option>{statuses.map((item) => <option key={item} value={item}>{item}</option>)}</select><select aria-label="Filter by follow-up timing" value={followUpFilter} onChange={(event) => setFollowUpFilter(event.target.value)} className="min-h-11 rounded-xl border border-slate-200 bg-white px-3 text-sm font-medium"><option value="all">All follow-ups</option><option value="action">Needs action</option><option value="overdue">Overdue</option></select></div>{visible.length ? <div className="divide-y divide-slate-100">{visible.map((lead) => { const label = followUp(lead); return <div key={lead.id} className="grid gap-3 p-5 lg:grid-cols-[1.4fr_1fr_auto_auto] lg:items-center"><div><div className="flex flex-wrap items-center gap-2"><a href={`/dashboard/leads/${lead.id}`} className="font-semibold text-slate-950 hover:text-sky-700 hover:underline">{lead.website_url}</a><span className={`rounded-full px-2 py-0.5 text-[10px] font-bold uppercase tracking-[.08em] ${priorityStyle[lead.priority]}`}>{lead.priority}</span></div><p className="mt-1 text-sm text-slate-500">{lead.email} · {new Date(lead.created_at).toLocaleDateString()}</p>{lead.tags.length > 0 && <div className="mt-2 flex flex-wrap gap-1">{lead.tags.slice(0, 4).map((tag) => <span key={tag} className="rounded-md bg-slate-100 px-2 py-0.5 text-[11px] font-medium text-slate-600">{tag}</span>)}</div>}</div><p className="text-sm text-slate-600">Performance {lead.performance ?? "—"} · SEO {lead.seo ?? "—"}</p>{label ? <span className={`w-fit rounded-full px-3 py-1 text-xs font-semibold ring-1 ${label.style}`}>{label.text}</span> : <span className="text-xs text-slate-400">No follow-up set</span>}<LeadStatusSelect leadId={lead.id} initialStatus={lead.status} /></div> })}</div> : <div className="p-8 text-sm leading-6 text-slate-600">No leads match these filters. Try clearing a filter or search term.</div>}</section></div>
}
