"use client"

import { BarChart3, CalendarClock, CircleAlert, TrendingUp } from "lucide-react"
import type { DashboardLead } from "@/components/dashboard-lead-list"

const dayKey = (value: Date) => `${value.getFullYear()}-${String(value.getMonth() + 1).padStart(2, "0")}-${String(value.getDate()).padStart(2, "0")}`
const today = new Date(new Date().getFullYear(), new Date().getMonth(), new Date().getDate())

export function DashboardAnalytics({ leads }: { leads: DashboardLead[] }) {
  const won = leads.filter((lead) => lead.status === "Won").length
  const conversion = leads.length ? Math.round((won / leads.length) * 100) : 0
  const thisMonth = leads.filter((lead) => new Date(lead.created_at).getMonth() === new Date().getMonth())
  const average = (key: "performance" | "seo") => { const values = thisMonth.map((lead) => lead[key]).filter((value): value is number => typeof value === "number"); return values.length ? Math.round(values.reduce((a, b) => a + b, 0) / values.length) : "—" }
  const dueToday = leads.filter((lead) => lead.follow_up_at === dayKey(today) || lead.re_audit_at === dayKey(today))
  const overdue = leads.filter((lead) => (lead.follow_up_at && lead.follow_up_at < dayKey(today)) || (lead.re_audit_at && lead.re_audit_at < dayKey(today)))
  const highPriority = leads.filter((lead) => lead.priority === "high" && lead.status !== "Won" && lead.status !== "Not a fit")
  const focus = [...overdue, ...dueToday, ...highPriority].filter((lead, index, all) => all.findIndex((item) => item.id === lead.id) === index).slice(0, 5)
  const views = [["all", "All leads"], ["high", "High priority"], ["follow-up", "Needs follow-up"], ["won", "Won leads"]]
  return <section className="mt-8 space-y-5"><div className="rounded-2xl bg-slate-950 p-6 text-white shadow-sm"><div className="flex flex-wrap items-center gap-3"><CircleAlert className="size-5 text-sky-300" /><div><p className="text-sm font-bold uppercase tracking-[.14em] text-sky-200">Today&apos;s command center</p><h2 className="mt-1 text-xl font-bold">Focus on the work most likely to move forward.</h2></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><a href="/dashboard?view=follow-up" className="rounded-xl bg-white/10 p-4 transition hover:bg-white/15"><p className="text-sm font-semibold text-sky-100">Overdue follow-ups</p><p className="mt-1 text-3xl font-bold">{overdue.length}</p></a><a href="/dashboard?view=high" className="rounded-xl bg-white/10 p-4 transition hover:bg-white/15"><p className="text-sm font-semibold text-sky-100">High-priority leads</p><p className="mt-1 text-3xl font-bold">{highPriority.length}</p></a><a href="/dashboard?view=follow-up" className="rounded-xl bg-white/10 p-4 transition hover:bg-white/15"><p className="text-sm font-semibold text-sky-100">Due today</p><p className="mt-1 text-3xl font-bold">{dueToday.length}</p></a></div>{focus.length > 0 ? <div className="mt-5 border-t border-white/15 pt-5"><p className="text-sm font-semibold text-sky-100">Next actions</p><div className="mt-3 flex flex-wrap gap-2">{focus.map((lead) => <a key={lead.id} href={`/dashboard/leads/${lead.id}`} className="rounded-lg bg-white px-3 py-2 text-sm font-semibold text-slate-950 transition hover:bg-sky-100">{lead.website_url}</a>)}</div></div> : <p className="mt-5 border-t border-white/15 pt-5 text-sm text-sky-100">No urgent follow-ups today. Use this time to review new audits or improve your outreach templates.</p>}</div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-center gap-3"><BarChart3 className="size-5 text-sky-700" /><div><p className="text-sm font-bold uppercase tracking-[.14em] text-sky-700">Dashboard analytics</p><h2 className="mt-1 text-xl font-bold">Lead and score trends</h2></div></div><div className="mt-5 flex flex-wrap gap-2">{views.map(([key, label]) => <a key={key} href={key === "all" ? "/dashboard" : `/dashboard?view=${key}`} className="rounded-full border border-slate-200 bg-slate-50 px-3 py-2 text-sm font-semibold text-slate-700 transition hover:border-sky-300 hover:bg-sky-50 hover:text-sky-800">{label}</a>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-3"><article className="rounded-xl border border-slate-200 p-5"><p className="text-sm font-semibold text-slate-600">This month&apos;s audits</p><p className="mt-2 text-3xl font-bold">{thisMonth.length}</p></article><article className="rounded-xl bg-slate-950 p-5 text-white"><TrendingUp className="size-5 text-sky-300" /><p className="mt-3 text-sm font-semibold text-sky-100">Lead conversion</p><p className="mt-1 text-3xl font-bold">{conversion}%</p></article><article className="rounded-xl border border-emerald-200 bg-emerald-50 p-5"><p className="text-sm font-semibold text-emerald-900">Current averages</p><p className="mt-2 text-lg font-bold text-emerald-950">Mobile {average("performance")} · SEO {average("seo")}</p></article></div></div></section>
}
